'use strict';

var express = require("express");
var router = exprss.Router();

//Get /getData
router.get("/", function(req, res){
	res.json({response: "Lack of Parameter"});
})
//POST /getData
router.get("/", function(req, res){
	res.json({response: "Lack of Parameter"});
})

module.exports = router;